name: Generate Architecture Explainer
on:
  push:
    branches: [main, staging]
  workflow_dispatch:
permissions:
  contents: write
jobs:
  generate:
    uses: Bad-Robots/Workforce/.github/workflows/generate-explainer-reusable.yml@16074713aab17acfb31b5cf205754e5457050381
    with:
      caller_repo: ${{ github.repository }}
      base_branch: main
      workforce_ref: 16074713aab17acfb31b5cf205754e5457050381
      skip_gate: true
    secrets:
      BYPASS_API_KEY: ${{ secrets.BYPASS_API_KEY }}
